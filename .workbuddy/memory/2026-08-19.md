# 2026-08-19 工作日志

## 新增 WORKING_PRINCIPLES.md 协作规范（commit 7786669 → dbeb08a）
- 用户要求把「对话中的工作原则」总结成 md 文件，并确认 AGENTS.md 是否已写明。
- 结论：AGENTS.md **没有**专门的协作原则章节（只有技术流程/9连败教训/常见坑），原则散落在记忆里。
- 创建 `WORKING_PRINCIPLES.md`：①核心分工（本地改代码 vs 云 agent 跑长实验）②本地能/不能做什么 ③云 agent 提示词 5 项强制要求（commit+push/禁自造脚本/只跑指定轮次/结果贴回复/拉取方式）④实验验证铁律（对局验证 500+ 局，禁 loss/val_top1/节点数代理，小样本 Elo 不可信，禁理论分析判 bug）⑤已封板 9 连败 ⑥git 协作（云 agent 跑实验期间本地避免 push CNB）⑦环境速查。
- AGENTS.md 顶部加引用「先读 WORKING_PRINCIPLES.md」。

## 云 agent 产出 eval 标签训练集（commit 6d0d34f，已合并到本地）
- 按 cnb_eval_labels_prompt.md 跑完：Stockfish depth10 深搜 eval 标签，20 万局面 + 对称增广 = **40 万行**，`data/eval_labels.txt.gz`。
- **质量验证通过**（trust but verify）：
  - 400,000 行；均值 0.0 / 中位数 0.0（增广后完全对称，无白方偏置）；
  - min/max ±804（合理范围）；|eval|>3000 占比 0%（无杀棋噪音）；
  - 抽样：镜像 FEN eval 正确取反（-9 → 9）。

## git 协作实践
- 本地 push 被拒（CNB 有云 agent 的 eval 标签提交）→ stash .gitignore 改动 → `git pull --rebase cnb main` → stash pop → push 成功。
- 发现外部会话新增：data-etl/（NNUE ETL，Rust）、train_nnue.py/qa_nnue.py/parquet_dump.py、.gitignore 加 NNUE 忽略规则（均未跟踪或已改）。

## 项目最新状态（AGENTS.md 已记录 2026-08-19）
- NNUE 数据管线 **M0–M2 已跑通**（HF Lichess/chess-position-evaluations → parquet_dump.py → data-etl(Rust, HalfK-768+.scnn) → train_nnue.py，5.66M 唯一局面）。
- **M3 本地完成**（commit 078e5e3）：nnue.rs 静态 CNN 推理 + eval.rs 接入（UCI `Eval` 开关）+ match.py `--eval-a/b` + golden test（Rust vs PyTorch 逐位一致，随机权重 max_diff=0.0 PASS）。教师标签 40 万行经 data-etl `--stm-cp` → 30.2 万唯一局面。
- **M4 云 agent 完成**（commit 70e3a7f）：训练 + golden test PASS + pilot 40 局 **0-40**（LOS=1.0）。根因 = 静态 NNUE eval ~100x 慢 → 搜索深度塌陷（300ms 只能浅搜），按决策门跳过 500 局。
- **结论**：M3–M4 全链路（数据→训练→golden→集成→对局）已跑通；静态 CNN 推理太慢，**下一步 = NNUE 增量推理**（eval 成本降到 ~µs 级）才能真正谈棋力。
- **凭据安全**（commit 193afb9）：AGENTS.md 明文 PAT/SSH 密码已剥离，改环境变量 + 本机凭据存储；推送流程 = cnb + GitHub 双端一并推（GitHub 连不上走代理）。
- 凭据：cnb token（duwenfeng）、GitHub fine-grained PAT（Contents Read+Write）均存本机 osxkeychain，**勿写回仓库**。
