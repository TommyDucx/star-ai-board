# 2026-08-15 工作日志

## 数据来源换血方向最终否定（400 局大样本坐实）
- 云 agent 跑完 400 局大样本对弈（deep vs old），结果：**old 145 胜 / 117 和 / deep 138 负 → deep 胜率 49.12%，Elo −6.1，LOS 仅 33.9%，CI [−34.9, +22.6] 大幅跨 0**。
- **本地 96 局的 +47.3 Elo（LOS 93.7%）是噪声假阳性，未复现**——小样本正向结果被大样本拉回零附近甚至略负。
- 三个换血方向最终全部否定：
  1. Stockfish 教师：−34.9 Elo（40 局）
  2. 纯 α-β 自蒸馏：−34.9 Elo（40 局）
  3. deep（policy 开启 + 深搜索 1000ms）：+47.3（96 局噪声）→ **−6.1（400 局坐实否定）**
- 云 agent 点出的关键疑点：deep 标签是 movetime 1000ms（深搜索），但对弈验证用 300ms（浅搜索），存在「训练-测试搜索深度错配」——深搜索标签指导浅搜索的初衷未实现。
- **终极结论：Policy 引导（数据侧）已到天花板，任何改数据/标签的方向收益 < 噪声水平。唯一确定性收益仍是搜索侧（Lazy SMP +51 Elo，已部署）。**

## 教训（重要，供后续）
- **小样本（40~96 局）对弈的 Elo 完全不可信**，+47 Elo 都能是纯噪声；要分辨 <50 Elo 差异必须 ≥400 局。
- 之前两轮 RL（+18.5 / +1.2 Elo）也都是 <500 局、未显著，同样属噪声区间——「自博弈 RL 闭环」整体收益未证实，而非只是"到平台期"。

## 本次同步到 GitHub
- 用代理 `127.0.0.1:1087` 成功 push 32 个提交到 `origin`（GitHub star-ai-board），本地/GitHub/CNB 三端同步。用户 2026-08-15 重新启用了该代理（见用户级 MEMORY.md）。

## 搜索侧优化：TT age 换代统一（2026-08-15 15:20）
- 选定方向：接受「Policy 数据侧天花板」，转向搜索侧确定性优化。
- 实现：`search.rs` 中 `new_generation()` 从「迭代加深每个深度调用」改为「`search()` 外层每次 go 调用一次」，修复 Lazy SMP 下多线程重复换代（4线程×11深度≈44次/go）导致 age 快速递增、命中率下降的问题。
- 编译通过 + 单/多线程冒烟正常（bestmove b1c3）。commit `449e4eb` 已推 CNB。
- 验证提示词 `cnb_tt_age_prompt.md`（commit `5c0e0a1`）：云 agent 编译新旧两版（HEAD vs HEAD~1），**双方 Threads=4**、400 局、同 policy 对弈坐实收益。

## Texel tuning 工具链（2026-08-16，选定方向起步）
- 用户建议四大优化方向（Policy/History 解耦、IID、奇异延伸、Texel、NNUE、movegen、TM），经评估后选定 **Texel Tuning** 起步（确定性 50-100 Elo，契合「改代码 + 云 agent 跑实验」分工）。
- 交付 4 个脚本 + eval.rs 重构：
  - `texel_eval.py`：Python 参数化 eval（精确复刻 eval.rs）+ 25 维线性特征提取；**golden test 已通过**（Python 与 Rust `eval_stm` 在 5 个局面逐字节一致，临时给 main.rs 加 `eval` 命令验证后已恢复）。
  - `texel_tune.py`：logistic 回归（Adam）调 25 个标量参数（子力值/兵形/双象/车线/通路兵/王盾/王翼进攻），PST 表留第二阶段。
  - `texel_data.py`：从自对弈 PGN 提取「安静局面 + 结果」（跳过开局/被将/吃子后）。
  - `texel_apply.py`：把调参结果写回 eval.rs 的具名常量（正则替换，已验证）。
  - `my-engine/src/eval.rs` 重构：内联魔法数字提成 14 个具名常量（DOUBLED_PENALTY 等），值不变，行为不变。
- 提交：`016a36c`（工具链 + 重构）、`1d695bd`（提示词），已推 CNB。
- 云 agent 提示词 `cnb_texel_prompt.md`：自对弈 5000 局 → 提取 → 调参 → 写回 → 编译新旧 → 400 局对弈验证。

## 待办（下一步）
- 云 agent 跑 Texel tuning 全流程，回来分析调参收益（loss 下降 + 对弈 Elo）。
- 若验证正向，采纳新 eval 参数并同步树莓派。
- 仍挂起：TT age 优化对弈验证（cnb_tt_age_prompt.md，云 agent 尚未跑）。
- 未采纳的 deep policy 产物已恢复；`/tmp/policy_deep.bin` 留档。
