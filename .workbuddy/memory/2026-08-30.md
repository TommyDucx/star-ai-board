# 2026-08-30 工作日志

## 账号系统前端与站点风格统一（本会话）
- **背景**：用户要求后台账号系统前端与网站其他已完成页面（main.html/chess.html，Kylin 暗色主题 + motion 动效体系）风格、互动效果完全一致。树莓派关机后重启，22/8765 端口已恢复连通。
- **admin.css 对齐**：设计令牌全部换成站点同款（--ink:#080a09 等 + --danger/--warn 语义别名）；按钮改为站点 .btn 同款（ui-monospace 11px、无背景、letter-spacing .08em、transition .15s）；顶栏底色 --surface → --ink；全扁平化（0 圆角）规则置于文件末尾覆盖具体规则；`.magnetic{position:relative}`、`#sfxBtn{display:none}`（隐藏 motion.js 自动注入的音效开关）。
- **三个认证页**（login/register/reset）：加载 ../css/motion.css + ../js/motion.js?v=33，加 scanlines 层，卡片加 `enter-node magnetic ripple-host`，登录按钮加 ripple-host，内联 Motion.initCursorLine/initRipple/initMagnetic/staggerEnter。
- **SPA 主页面 index.html**：加载 motion.css + motion.js，加 scanlines + pixel-fault 层；强制改密弹窗文案 6 位 → 8-128 位含大小写数字；pw-save 按钮 ripple-host。
- **js/admin.js**：
  - 新增 `validPassword()`（8-128 位 + 大小写 + 数字，与服务端一致），用于改密弹窗/创建账号/个人资料改密三处（原来 6 位/8 位的宽松校验全部升级）。
  - `api()` 增加 403 `{mustChange:true}` 处理 → 自动弹强制改密弹窗（配合服务端 8718c80 硬拦截）。
  - 动效：init() 里初始化 Motion 三件套；每个 render 函数末尾调 `afterRender()`（= rescanFx + 视图切换时 animView）。
  - **关键细节**：`state.switching` 标志区分「视图切换」（重放 #view 的 enter-node 进场动画）与「看板 5s 自动刷新」（只 rescanFx，不重放动画，避免每 5 秒闪一次）；initMagnetic 只靠 resize 重扫 .magnetic，故 rescanFx 里 dispatch resize 事件（比重复调用 initMagnetic 更优，不会叠加监听器）。
- **验证**：node --check 通过；本地 PORT=8801 起服务冒烟——公开页 200、受保护资源未登录 302/401（预期）、登录后 index.html/admin.js/admin.css 全部 200、新 admin.js 含 validPassword/mustChange；三个公开页 motion 集成标记各 7 处。
- **杂项**：目录下有不相关的未跟踪文件（qa_fixes.md/qa_issues.md/session-ses_fe60.md/.write_test_tmp），未纳入提交。
- **提交与部署**：commit `3abf36f`（8 文件 +158/−35）已推 cnb + GitHub（代理 127.0.0.1:1087 + GIT_HTTP_VERSION=1.1）。树莓派部署：15KB 精简包（仅 6 个前端文件）tar→scp→解压→`sudo systemctl restart star-ai-board`，ActiveEnterTimestamp 08:42:49；线上验证：三个公开页 200、index.html 未登录 302（预期）、受保护 css 401（预期）、motion.css/motion.js 200、login.html 含 7 处动效标记且 login-card 带 `enter-node magnetic ripple-host`。

## 后台前端无头浏览器实测 + 6 类 bug 修复（本会话续）
- **测试方法**：用 agent-browser（本机 Chromium）连 192.168.0.107:8765 实测；hook window.fetch + 捕获 submit 事件 + click target + elementFromPoint 定位。
- **发现的 6 个真实前端 bug（已全部修复并部署）**：
  1. 后台 css/js 返回 401（handleRequest 把 /admin/* 静态资源也拦了）→ 加静态资源正则放行。
  2. 强制改密后看板空白 → showPwModal 成功分支改 `switchView(state.view)` 重渲染当前视图。
  3. 注册/重置按钮字体变 Arial → admin.css 加 `button,select,textarea,input{font-family:inherit}`。
  4. 看板定时器指数爆炸（setInterval 写在 renderDashboard 内，每次 tick 再建 interval）→ 移入 switchView 仅设一次，切换时 clearInterval。
  5. 浏览器 7 天强缓存旧 JS（serveAdminFile 对 js/css 设 max-age=604800）→ 改为 `no-cache`。
  6. 注册提交点不动：根因是矮视口(577px)下 flex 居中把提交按钮挤到折叠线以下 + Playwright 自动滚动未生效（磁吸非元凶，已排除）。**产品逻辑正确**（requestSubmit 与滚入视口后点击均成功注册跳转）。修复：admin.css `.login-body` 改 `align-items:safe center`（内容溢出时不上下裁切），文档自然滚动可达按钮。
- **关键判定**：headless 下普通 click 在矮视口仍失败属测试器+flex 局限；真实浏览器(≥700px)按钮可见即正常点击。不强删 magnetic（保持站点动效一致）。
- **提交与部署**：commit `3478611`（admin.js + public/admin/js/admin.js + public/admin/css/admin.css，+27/−5）已推 cnb + GitHub（代理+PAT）。树莓派部署：仅 admin.css 静态包 scp→解压（无需重启）；后端 admin.js 一并重新部署并 `sudo systemctl restart star-ai-board`（active）。线上回归：登录 admin/StarAdmin2026→index.html 正常；注册 滚入视口点击→/api/register 200→index.html 正常。
- **注意**：AGENTS.md 含明文凭据（PAT+SSH 密码），本会话未提交（避免 secret scanning），仍留待迁出仓库。

## 首页「管理后台」入口可见性 + 引擎文案 + 缓存失效（本轮）
- **根因闭环**：首页管理后台入口前端早就做好（`<a href="/admin/login.html" class="admin-entry" data-nav>` + wireNav 捕获委托）。此前「点不动/像没做」是因为 server.js 对 css/js 设 7 天强缓存（max-age=604800），而 main/review/index 三页引用 `site.css` 没带 `?v`，浏览器缓存了**旧版 site.css**（其中 .topbar 是 `static`），顶栏塌进文档流、按钮沉到页底不可点。
- **修复**：① server.js 对 .html/.css/.js 改 `no-cache`（补齐 3478611 之外遗漏的静态资源缓存策略）；② 给 main/review/index/go/chess 五页 `site.css` 引用统一加 `?v=32` 强制失效旧缓存。
- **部署**：仅 5 个 html + server.js 打 23KB 包 → scp → 解压 → `sudo systemctl restart star-ai-board`（active）。**未**覆盖 public/engines、public/stockfish、public/reckless 等 ARM 二进制。
- **验证**：agent-browser 实测点击「管理后台」→ 跳转到 `/admin/login.html`（S.T.A.R.·ADMIN 登录页）；curl 确认线上 main.html 引 `site.css?v=32`、site.css 含 `position:sticky` topbar、响应头 `Cache-Control:no-cache`。
- **引擎文案**：main.html 顶栏 `MY-ENGINE · STOCKFISH · KATAGO`、hero「自研 my-engine 与 Stockfish 驱动」、foot「KATAGO + MY-ENGINE + STOCKFISH」。首页无数字引擎计数（动态计数在后台 `/api/admin/metrics` → engineStatus()），引擎数从 2（SF+KataGo）变 3（含自研 my-engine）。
- **管理员账号**：`admin` / `StarAdmin2026`（Pi 实测 POST /api/login 返回 200 + 会话 cookie，mustChange=false）。Bug6 测试残留账号 `aliceX/bobY/erinW`（member）待清理。
- **提交**：server.js + 5 个 html + 本日志；AGENTS.md 仍不提交。

## 注册简化（删验证码）+ 用户改名 + 账号系统安全审计（续）
- **需求**：删验证码系统，注册只需用户名+密码；面板里用户可改名；检查账号系统漏洞。
- **后端 admin.js（779→657 行）**：
  - 删除 `/api/auth/send-code`、`/api/me/verify`、`/api/auth/request-reset`、`/api/auth/reset-password` 及 auth_codes/reset_tokens 存储、sendMail、codeCooldown、validContact、ctEq。
  - `/api/register` 简化为仅 username+password（contact/code 字段即使传了也被忽略）。
  - `/api/me` PUT 新增改用户名（RE_USERNAME 校验 + 大小写不敏感唯一性 + 同步所有设备会话的显示名）；**改密必须带 currentPassword**（原来不验当前密码即可改密 = 会话劫持即可锁死账号，已修）。
  - findUser 只按用户名查登录；normalizeUser 剥离 email/phone/isVerified/loginFails 字段；status 枚举收敛为 active/suspended；旧 accounts.json 启动时自动迁移。
- **前端**：register.html 只剩用户名/密码/确认密码；profile 视图删「验证联系方式」面板、增「修改用户名」面板、改密加当前密码框；账号表删邮箱/手机列和 unverified 状态选项；**删除 reset.html**，login.html「忘记密码」改为提示联系管理员（管理员可在账号管理里改密）。
- **安全审计结论（已修）**：
  1. **严重**：自助找回密码把 reset token 直接放 API 响应里返回（dev 模式）→ 任何人可重置任意账号密码 = 完整账号接管。已随验证码系统整体移除该流程。
  2. **高**：/api/me PUT 改密不验证当前密码。已加 currentPassword 校验（profile 面板 + 强制改密弹窗都加了当前密码框）。
  3. **中**：accounts.json/sessions.json 0644 可被本机其他用户读（密码哈希 + 会话 token 明文）。已加 writePrivate() 落盘 0600，Mac 和 Pi 两侧均已 chmod。
  4. **加固**：admin.js serveAdminFile 与 server.js 静态处理的 startsWith 均加 path.sep 防同前缀目录绕过。
- **审计结论（确认无问题/已知接受）**：CSRF 被 SameSite=Lax + JSON Content-Type（跨站无法带该头触发预检且服务端无 CORS）挡住；XSS 有 esc() + 用户名白名单正则；登录限流 5/min(IP+账号)、注册限流 10/10min；登录有 DUMMY_HASH 防时序枚举、scrypt+盐；admin/ 数据文件均已 gitignore 且未入库。**遗留提醒**：站点经 cloudflared 公网隧道暴露，注册完全开放（任何人可注册 member，权限仅 profile/session），如收到垃圾注册可在 nginx/隧道层加访问控制或后续加邀请码开关。
- **测试**：本地 8801 + Pi 8765 双端冒烟：注册（仅用户名密码）✓、旧端点 401/404 ✓、改名 ✓（含会话同步）、无/错当前密码改密 403 ✓、正确当前密码改密 ✓、大小写不敏感重名 409 ✓、admin 登录 ✓、reset.html 302 ✓、register.html 无验证码残留 ✓、agent-browser 实测 profile 面板渲染无 JS 错误 ✓。
- **部署**：admin.js + server.js + admin 三个 html + admin.js 前端 → tar 29KB scp → Pi 解压、删 reset.html/auth_codes.json/reset_tokens.json、chmod 600、restart（active）。
- **遗留**：Pi 测试账号 aliceX/bobY/erinW 仍在（用户未答复是否删）；GitHub 两 PAT 均失效未推。

## 国际象棋经典题型练习模块（站点第二个板块）
- **需求**：新增国际象棋经典题型练习（双重攻击/牵制/闪击/消除防御/底线杀/残局杀等），按难度阶梯分档；用户主动要求**不自己出题，直接用现成开源题库**，最终保留 ~3 万题且全部题型覆盖。
- **开源题库**：直接采用 **lichess 官方题库**（`database.lichess.org/lichess_db_puzzle.csv.zst`，304MB，CC0 公有领域，600 万题，含 FEN/UCI moves/评分/Themes/人气/出题局 ID，解法官方保证「每一步都是唯一最优」）。**关键经验：自己出题远不如直接拿开源题库——手搓 27 题就 FEN 笔误/几何误判/材料不平衡频发，而 lichess 数据已引擎验证、按主题和评分筛一遍就是高质量四级题库。**
- **数据管线**：`data-etl/build_puzzles.py`（streaming 解压 zst → CSV 流式遍历 → 按 lichess rating 分四档 [beginner<1200 / elementary 1200-1600 / intermediate 1600-2100 / advanced≥2100] + 每(主题×档)最多 300 题保证多样性 → FEN 应用首着得玩家行棋局面 → moves 取玩家解法 → JSON 输出）。最终：32000 题、7.6MB、45 种主主题、73 个 lichess 主题标签、四档均匀（入门 7975 / 初级 8071 / 中级 8169 / 高级 7785）。300 题抽样走法链完整 0 损坏。`public/js/lib/chess.min.js` 是 UMD 老版本（`exports.Chess = Chess`），Node 模拟须用解构 `const {Chess} = require(...)` 而非默认导入。
- **前端**：`public/tactics.html`（棋盘 460×460 + 难度阶梯 + 主题标签 + 提示/重做/下一题 + 解析区）+ `public/js/tactics.js`（拖拽落子 → 匹配 solution → 对手自动应 →wrong 抖动回弹；mateIn1 允许多种杀着；localStorage 按档存档已做题 ID → 进度条与「已完成 x/y 题」自动更新）。棋盘组件复用 `chessboard.min.js` + `chess.min.js`（与 chess.html 同款）。
- **入口**：`public/main.html` 改为 2×2 网格，新增「TACTICS / 经典题型练习」卡片（与围棋/国际象棋/对局复盘并列）。
- **测试**：本地 Node 模拟走解法链 20/20 通过；agent-browser 截图确认棋盘渲染、主题标签、进度计数（0/7975）正确；服务侧 `puzzles.json` 已部署到 Pi（`public/data/puzzles.json` 7.6MB，静态服务）。**注：page snapshot 显示"已完成 0 / 7975 题"——和入门档总数对得上，证明题库分档和前端过滤逻辑正确。**
- **部署**：tactics.html + js/tactics.js + data/puzzles.json + main.html → tar 1.7MB scp → Pi 解压（无需重启）；commit `7cc5412` 已推 cnb（GitHub PAT 仍失效未推）。
- **可扩展**：要扩到几千题只需放宽每(主题×档)配额或去掉上限；要换题源（如 chess.com）改 `build_puzzles.py` 的 CSV 列名即可。

## 首页布局定稿 + GitHub 推送恢复（续）
- **首页最终形态**（用户多次迭代后定稿）：`public/main.html` 恢复 b2a49a1 时代 hero+卡片风格——**左「围棋」卡、右「引擎对战」卡（12 引擎 1320-2600 难度）、下方「经典题型练习」横幅**；移除对局复盘卡片（review.html 保留但无入口）。中间试过「侧栏+详情卡」布局（6758358）被用户否决。
- **引擎对战页面 = 原 chess.html 原样**：与 b2a49a1 版本 `git show b2a49a1:public/chess.html` diff=0，全程未改。
- **tactics 页筛选（chess.com 风格）**：主题多选 chip（14 个，按题数降序：残局4751/短组合4443/将杀3690/压倒性3046/中局2617/单步1744/一步杀1739/两步杀1458/长组合1344/advantage1188/master1006/通路兵870/王翼进攻702...）+ 评分 min/max 输入 + 重置；渲染「已完成 x/y 题（筛选后，共 z）· 评分 min-max」。
- **GitHub 推送恢复（2026-08-30）**：用户重新提供 fine-grained PAT `github_pat_11BPEZ...`，**关键坑：推送 URL 用户名字段必须用 `TommyDucx` 而非 `x-access-token`**（后者报 "Invalid username or token"）。用法：`export http_proxy=http://127.0.0.1:1087 https_proxy=... GIT_HTTP_VERSION=1.1` + `git -c "url.https://TommyDucx:<TOKEN>@github.com/.insteadOf=https://github.com/" push origin main`（不改 remote）。推送前可用 `curl -H "Authorization: Bearer $TOKEN" https://api.github.com/user` 验证 token。5+1 个提交全部推到 GitHub，三端（cnb/origin/本地）同步于 `c4b95b1`。
- **agent-browser 点击坑**：magnetic 磁吸位移会导致坐标点击不准（卡片点击跳转测不出），改用 `agent-browser eval 'document.querySelector("a[href=...]").click()'` 程序化点击验证导航。
- 提交：`6758358`（侧栏，后被否决）、`c4b95b1`（首页定稿）。

## 首页最终版 + 每日残局/连击/教程/提示高亮（续）
- **首页定稿（再次迭代）**：保留侧栏两栏目（⚔ 引擎对战 / ⧉ 经典题型练习），围棋不放侧栏——通过引擎对战内容区左卡片进入 go.html；引擎对战视图 = b2a49a1 风格 hero + 左围棋卡 + 右国际象棋卡；引擎数量文案改为「12 个本地引擎已就绪」（不再列名字）；chess.html 全程零改动（diff=0）。commit `52bb158`。
- **每日残局**（tactics 页顶部标签「题库练习/每日残局」）：按日期哈希 `hashInt("d:"+dateKey()) % pool.length` 从 32000 题库（入门+初级池）确定性取题，每天 00:00 自动换题、离线可用；题面显示「日期 · 今日未完成/已完成 · 难度分」；解题自动 bump 连击。
- **连击 streak + 徽章**：localStorage `tactics.streak` {cur,best,last}；今天/昨天续签 +1，否则清零；徽章 3/7/14/30 天（铜·起步/银·坚持/金·入流/钻·大师），best 达标解锁；题库练习和每日残局解题都会 bump。
- **提示棋盘高亮**：chessboardjs 0.3 无 highlight API → 手动给 `[data-square]` 加 CSS 类：起点 `.tac-hl-from`（蓝框 4px）+ 终点 `.tac-hl-to`（黄框），落子/换题时清除。
- **引擎对战教程**（chess.html）：侧栏按钮区加「教程」按钮（替换原「复盘」按钮位置）→ 覆盖层弹窗六步教学（①发起对战 ②选择引擎 ③选择难度 ④读评估分 ⑤理解胜败规则 ⑥复盘与设置），点蒙层或 ✕ 关闭，移动端 `width:min(620px,100%);max-height:86vh;overflow-y:auto` 自适应。原有 12 引擎列表/布局零改动。
- **agent-browser 坑**：内联 onclick 按钮坐标点击不可靠（点击后 display 仍 none），用 `agent-browser eval 'openTutorial()'` 验证功能本身正常；真实浏览器无此问题。
- 提交 `528be59` 已推 cnb + GitHub（三端同步）。
