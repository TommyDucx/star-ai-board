# 2026-08-20 工作日志

## 工作模式切换：我就是云端 agent
- 用户明确：当前对话的 agent = CNB 云端 agent（Linux /workspace），**以后不再生成 cnb_*.md 云端提示词**，长任务（训练/大规模对弈/打标签/ETL）直接本环境跑完；跑完按规范 commit+push 结果 + 关键输出贴回复。
- 已写入长期 memory（工作分工条目更新）。

## 新建 CLOUD_AGENT_STATUS.md（云端状态速查）
- 目的：CNB 工作区随会话关闭，每段新对话的 agent 需 30 秒接上进度。
- 内容：①新工作模式 ②实验验证铁律 ③当前进度（NNUE M0–M6 一览 + M6 下一步）④环境速查 ⑤维护规则。
- 规则：每完成一步立即更新该文件 + 与结果同 commit+push。

## 考古确认：M6 HalfKP 重训已跑完但未汇报（关键发现）
- HEAD = `67ebb8f`（HalfKP 标签视角 bug 修复：fixed-color 特征需 eval_white 标签）。
- 工作区存在 `my-engine/policy/nnue_hkp.bin`（version=3，2,888,929 参数，~11.6MB）+ `policy_nnue_hkp.pt` → 说明**已用修复后 HEAD 重训完 HalfKP**（5M 样本源，`data/nnue/train_hkp.scnn`）。
- **缺口**：修复后**新 corr 未知**（第一次 buggy 标签 corr 0.21 已作废，`5d1c032`）；nnue_hkp.bin 未评估、未 commit。
- **下一步**：① 评估 nnue_hkp.bin 的 val corr（复现 val 划分 seed=42 / 前5M / 5%）→ ② corr>0.80 才投入引擎集成（nnue.rs v3 + search 王桶边界 / 白王换桶全量重算）→ ③ golden + pilot。corr≤0.75 则停。
- 环境坑：云端 **torch 未装**（需 `pip install --break-system-packages torch`）；numpy 已装。
